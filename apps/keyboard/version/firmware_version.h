#ifndef MOSAIC_KEYBOARD_FIRMWARE_VERSION_H
#define MOSAIC_KEYBOARD_FIRMWARE_VERSION_H

/** @brief Generated numeric firmware version as a string. */
extern const char keyboard_firmware_version[];
/** @brief CMake build configuration name. */
extern const char keyboard_firmware_build_type[];
/** @brief Firmware build timestamp. */
extern const char keyboard_firmware_build_time[];
/** @brief Git describe string at build time. */
extern const char keyboard_firmware_git_describe[];
/** @brief Short Git commit identifier at build time. */
extern const char keyboard_firmware_git_hash[];
/** @brief Git branch recorded at build time. */
extern const char keyboard_firmware_git_branch[];

#endif
